{
    "name": "vscode-ical-editor",
    "publisher": "marumasa",
    "displayName": "%displayName%",
    "description": "%description%",
    "icon": "images/icon.png",
    "l10n": "./l10n",
    "repository": {
        "type": "git",
        "url": "https://github.com/malken21/vscode-ical-editor.git"
    },
    "version": "1.1.0",
    "engines": {
        "vscode": "^1.85.0"
    },
    "categories": [
        "Other"
    ],
    "activationEvents": [],
    "main": "./out/extension.js",
    "contributes": {
        "customEditors": [
            {
                "viewType": "marumasa.icalEditor",
                "displayName": "%displayName%",
                "selector": [
                    {
                        "filenamePattern": "*.ics"
                    }
                ],
                "priority": "default"
            }
        ],
        "commands": [
            {
                "command": "marumasa.icalEditor.toggleText",
                "title": "%command.toggleText%",
                "icon": "$(file-code)"
            }
        ],
        "menus": {
            "editor/title": [
                {
                    "command": "marumasa.icalEditor.toggleText",
                    "when": "activeCustomEditorId == 'marumasa.icalEditor'",
                    "group": "navigation"
                }
            ]
        }
    },
    "scripts": {
        "vscode:prepublish": "npm run compile",
        "compile": "tsc -p ./",
        "watch": "tsc -watch -p ./"
    },
    "devDependencies": {
        "@types/vscode": "^1.85.0",
        "@types/node": "^18.0.0",
        "typescript": "^5.3.0"
    }
}